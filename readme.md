# AWS Credential to Enviornment Script

A semi-automated script that logs you into AWS SSO then automatically exports your credentials to an .env file in the same directory as the script.

# How to run

On Windows:
- Run `run_windows.bat`

On Linux/macOS:
- Run `run_unix.sh`
- May need to run `chmod +X run_unix.sh` beforehand
